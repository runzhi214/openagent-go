[package]
name = "example-model-switch"
version = "0.1.0"
edition = "2021"
description = "Example agent:observers plugin that detects 401 and rotates model"

[lib]
crate-type = ["cdylib"]

[dependencies]
openagent-pdk = { path = "../../../plugin/pdk/rust" }
